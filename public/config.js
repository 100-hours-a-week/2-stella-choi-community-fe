const hostUrl = window.location.hostname === 'localhost'
    ? 'http://localhost:8080/api/'
    : 'http://3.34.50.126:8080/api/';

const staticUrl = window.location.hostname === 'localhost'
    ? 'http://localhost:8080/'
    : 'http://3.34.50.126:8080/';

const serverVersion = 'v1'